# This is an example of a .env file for a Next.js application using NextAuth.js with Shopify and Google OAuth.
# Make sure to replace the values with your actual credentials.

# Shopify OAuth
# You can get these values from your Shopify app settings.
# Make sure to set the redirect URL in your Shopify app settings to match your Next.js app.
SHOPIFY_CLIENT_ID=…
SHOPIFY_CLIENT_SECRET=…
SHOPIFY_STORE_DOMAIN=mon-store.myshopify.com

# NextAuth
# You can generate a secret using the command: openssl rand -hex 32
# Make sure to set the NEXTAUTH_URL to your app's URL.
# If you're running locally, it should be http://localhost:3000
# If you're deploying to Vercel, set it to your Vercel app's URL.
# If you're deploying to another platform, set it to your app's URL.
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=…

# Google OAuth
# You can get these values from your Google Cloud Console.
# Make sure to set the redirect URL in your Google app settings to match your Next.js app.
GOOGLE_CLIENT_ID=…
GOOGLE_CLIENT_SECRET=…